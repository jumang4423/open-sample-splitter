// splitSample helper functions
// split a sample into multiple samples based on silence, on file and threshold
import splitAudio from "split-audio-silence";

export const SplitSample = (
  sample: File,
  threshold: number
): Promise<File[]> => {};
